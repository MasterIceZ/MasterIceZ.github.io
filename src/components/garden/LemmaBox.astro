---
interface Props {
  title?: string;
}
const { title } = Astro.props;
---

<div class="border px-3 py-2 flex flex-col rounded-lg m-2 lemma-box">
  <p class="font-semibold">Lemma: {title ? title : ""}</p>
  <slot />
</div>
