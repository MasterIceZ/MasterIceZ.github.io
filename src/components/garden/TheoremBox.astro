---
interface Props {
  title?: string;
}
const { title } = Astro.props;
---

<div class="border px-3 py-2 flex flex-col rounded-lg m-2">
  <p class="font-semibold">Theorem: {title ? title : ""}</p>
  <div class="flex flex-row">
    <slot />
  </div>
</div>
